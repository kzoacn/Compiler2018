





default rel

global main

extern _IO_putc
extern _GLOBAL_OFFSET_TABLE_
extern stdout


SECTION .text   


SECTION .data   


SECTION .bss    


SECTION .text.startup 6

main:
        sub     rsp, 8
        mov     rsi, qword [rel stdout]
        mov     edi, 50
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 10
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 52
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 10
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 54
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 10
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 53
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 10
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 55
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 10
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 54
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 10
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 49
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 49
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 10
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 49
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 50
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 10
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 55
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 10
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 49
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 48
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 10
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 50
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 50
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 10
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 49
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 51
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 10
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 49
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 51
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 10
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 50
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 54
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 10
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 50
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 49
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 10
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 49
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 55
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 10
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 49
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 49
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 10
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 49
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 51
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 10
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 52
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 55
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 10
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 50
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 54
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 10
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 51
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 52
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 10
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 50
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 51
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 10
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 57
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 10
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 53
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 50
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 10
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 52
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 55
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 10
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 53
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 52
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 10
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 54
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 52
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 10
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 49
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 57
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 10
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 50
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 52
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 10
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 49
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 57
        call    _IO_putc
        mov     rsi, qword [rel stdout]
        mov     edi, 10
        call    _IO_putc
        xor     eax, eax
        add     rsp, 8
        ret



